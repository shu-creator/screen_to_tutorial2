# ============================================
# TutorialGen ローカル開発用 環境変数
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env
# ============================================

# --- データベース ---
DATABASE_URL=mysql://root:password@localhost:3306/tutorialgen

# --- 認証 ---
# none: 認証スキップ（開発用）、local: メール/パスワード認証
AUTH_MODE=none
# 本番環境では32文字以上の安全な値を設定してください
# JWT_SECRET=your-very-long-secret-key-at-least-32-characters

# --- LLM（画像分析） ---
# プロバイダー: openai / gemini / claude
LLM_PROVIDER=openai
LLM_API_KEY=sk-your-key-here
# LLM_MODEL=gpt-5.2          # 省略時はプロバイダーごとのデフォルト
# LLM_API_URL=               # 省略時はプロバイダーの公式エンドポイント

# --- TTS（音声合成） ---
# プロバイダー: openai / gemini
TTS_PROVIDER=openai
TTS_API_KEY=sk-your-key-here
# TTS_MODEL=gpt-4o-mini-tts   # 省略時はプロバイダーごとのデフォルト
# TTS_VOICE=nova               # alloy / echo / fable / onyx / nova / shimmer

# --- ストレージ ---
# ローカルファイル保存先（デフォルト: ./data/storage）
# STORAGE_PATH=./data/storage

# --- Vite開発サーバー ---
# LAN公開時のみ設定（デフォルトはlocalhost制限）
# VITE_ALLOWED_HOSTS=*
