# ============================================
# TutorialGen ローカル開発用 環境変数
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env
# ============================================

# --- データベース ---
DATABASE_URL=mysql://root:password@localhost:3306/tutorialgen

# --- 認証（現在はManus OAuth、Phase 4でローカル認証に移行予定） ---
# 本番環境では必須。開発環境では未設定でも起動可能
# OAUTH_SERVER_URL=https://your-oauth-server.example.com
# 本番環境では32文字以上の安全な値を設定してください
# JWT_SECRET=your-very-long-secret-key-at-least-32-characters

# --- LLM/TTS（現在の実装で使用される変数） ---
# Manus Forge経由で利用する場合:
# BUILT_IN_FORGE_API_URL=https://forge.manus.im
# BUILT_IN_FORGE_API_KEY=sk-your-forge-key
# OpenAI直接利用の場合（TTSフォールバック）:
# OPENAI_API_KEY=sk-your-openai-key

# --- LLM プロバイダー切り替え（Phase 6で実装予定） ---
# 実装後は以下の変数でプロバイダーを切り替え可能になります
# LLM_PROVIDER=openai          # openai / gemini / claude
# LLM_API_KEY=sk-your-key-here
# LLM_MODEL=gpt-5.2            # 省略時はプロバイダーごとのデフォルト

# --- TTS プロバイダー切り替え（Phase 7で実装予定） ---
# TTS_PROVIDER=openai           # openai / gemini
# TTS_API_KEY=sk-your-key-here
# TTS_VOICE=nova                # alloy / echo / fable / onyx / nova / shimmer

# --- ストレージ（Phase 5で実装予定） ---
# ローカルファイル保存先（デフォルト: ./data/storage）
# STORAGE_PATH=./data/storage

# --- Vite開発サーバー ---
# LAN公開時のみ設定（デフォルトはlocalhost制限）
# VITE_ALLOWED_HOSTS=*
